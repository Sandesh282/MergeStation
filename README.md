# MergeStation

Automatically tracks open-source pull request contributions across multiple repositories and generates a visual contribution graph.

## How It Works

Uses the GitHub GraphQL API to fetch all pull requests and generates an SVG contribution network.

## Setup

Coming soon.
